# Deploy to Render

For 'Static Site' deployment on Render:

1.  **Build Command:** `npm run build`
2.  **Publish Directory:** `dist`

> **Note:** The `Publish Directory` is the folder where Vite outputs the built assets. By default, this is `dist`.
